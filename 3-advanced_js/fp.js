const user = {
    name: 'Kim',
    active: true,
    cart: [],
    purchases: []
}

function addItemsToCart(items) {
    for (let item in items) {
        user.cart.push(item);
    }
}

function taxItems(items) {
    for (let item in items) {
        items
        user.cart.push(i);
    }
}