const user = {
    name: 'Kim',
    active: true,
    cart: [],
    purchases: []
}

function addItemsToCart(items) {
    user.cart.push(...items);
}

function taxItems(items) {
    for (let item in items) {
        items
        user.cart.push(i);
    }
}